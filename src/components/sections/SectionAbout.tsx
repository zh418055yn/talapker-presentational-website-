const SectionAbout = () =>{
    return(
        <section id = "about" className = "min-h-screen flex flex-col justify-center items-center bg-slate-50">
            O proekte
        </section>
    );
};

export default SectionAbout;