const SectionContacts = () =>{
    return(
        <section id = "contacts" className = "flex flex-col items-center min-h-screen bg-slate-50 justify-center">
            contacty
        </section>
    );
};

export default SectionContacts;